// enums
// ! enums for register toggle
enum TabEnum{home,cart,favorite,profile,quickOrder}

// view state
enum ViewState{busy,error,idle,success,exception}

// drp down enum
enum DropDownEnum {lang,category,product}

// alert dialog enums
enum DialogType{
  error,processing,notification
}